<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Excercise4"
        tools:targetApi="31">

        <!-- Thêm Graphics Activity với intent-filter MAIN và LAUNCHER -->
        <activity
            android:name=".graphics.Graphics"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Giữ MainActivity nhưng xóa intent-filter của nó -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Xóa intent-filter này nếu không muốn MainActivity là entry point -->
        </activity>

    </application>

</manifest>
